# SERVER.md — サーバーのスペックメモ（テンプレ）

> このリポジトリを動かしている**特定のサーバー1台**の情報を記録するテンプレ。
> 各サーバーごとに値を埋めて使う。
>
> ここに書くこと:
> - 物理スペック（GPU / CPU / RAM / SSD / OS）
> - そのサーバー固有の事情（GPU 世代の癖・ドライバの注意点 等）
> - 実測値メモ（VRAM 実使用量・コールドスタート時間 等）
>
> ここに書かないこと:
> - 一般的な手順 → [OPERATIONS.md](OPERATIONS.md)
> - リポジトリ全体の説明 → [README.md](../README.md)

---

## 物理スペック

| 項目 | 値 |
|---|---|
| GPU | `<GPU 型番を記入>` |
| GPU VRAM | `<合計 VRAM を記入>` |
| GPU Driver | `<ドライババージョンを記入>` |
| CUDA | `<CUDA バージョンを記入>` |
| CPU | `<CPU を記入>` |
| RAM | `<RAM 容量を記入>` |
| SSD | `<SSD 容量・モデルウェイト格納先を記入>` |
| OS | `<OS とバージョンを記入>` |

---

## このサーバー固有の事情

> 例: GPU 世代固有のクラッシュ問題・特定ドライババージョンの注意点・コールドスタートの体感時間 など。
> 何もなければこのセクションは削除しても良い。

### `<項目名を記入>`

`<内容を記入>`

---

## VRAM 実情（現行モデル + 現行設定での実測）

> モデル/設定を変えたらこの表を作り直す。
> 実測値は起動後に `docker compose logs vllm-primary` の KV cache usage 行で確認。

| 内訳 | サイズ（目安） |
|---|---|
| モデルウェイト | `<実測値を記入>` |
| KV キャッシュ | `<実測値を記入（GPU_MEMORY_UTIL の設定値に依存）>` |
| CUDA オーバーヘッド | 約 8 GiB（一般的な目安） |
| **空き** | `<実測値を記入>` |

---

## 接続情報

| 項目 | 確認方法 |
|---|---|
| Tailscale IP | `tailscale ip -4` |
| LAN IP | `bash scripts/show_connection_info.sh` |
| API Key | `.env` の `LITELLM_MASTER_KEY` |

> ⚠️ インターネットへの直接公開は禁止。リモート利用は必ず Tailscale 等の VPN 経由で。
